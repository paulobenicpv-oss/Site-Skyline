# Readdy Replica

Versão estática em HTML/CSS/JS inspirada no Readdy.ai — layout 4K, responsivo e com animações suaves.

## 🚀 Como usar
1. Baixe ou clone esta pasta `readdy-replica/`.
2. Abra o arquivo `index.html` no navegador para visualizar.
3. Para hospedar:
   - **GitHub Pages:** faça upload do repositório e ative Pages.
   - **Netlify:** arraste a pasta e solte no painel do Netlify.
   - **Vercel:** execute `vercel` no diretório e siga as instruções.

## 📁 Estrutura
- `index.html` — página principal  
- `style.css` — estilos e animações  
- `script.js` — interações básicas  
- `assets/` — imagens e ícones  

## 💡 Customização
Altere as cores em `:root` no `style.css` para mudar a paleta.
